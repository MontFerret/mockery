LET baseUrl = @baseUrl
LET page = DOCUMENT(baseUrl + "/scenarios/forms/", { driver: "cdp" })

INPUT(page, "#email", "demo@example.com")
CLICK(page, "#newsletter-form button[type='submit']")
WAIT_ELEMENT(page, "#form-result:not([hidden])")

RETURN INNER_TEXT(ELEMENT(page, "#form-result"))
