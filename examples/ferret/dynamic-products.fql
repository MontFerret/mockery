LET baseUrl = @baseUrl
LET page = DOCUMENT(baseUrl + "/scenarios/dynamic-products/", { driver: "cdp" })

WAIT_ELEMENT(page, "[data-testid='dynamic-products'][data-loaded='true']")

RETURN ELEMENTS_COUNT(page, "[data-testid='product-card']")
