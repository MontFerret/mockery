LET baseUrl = @baseUrl
LET page = DOCUMENT(baseUrl + "/scenarios/dynamic-products/basic/", { driver: "cdp" })

WAIT_ELEMENT(page, "[data-testid='dynamic-products-scenario'][data-state='loaded']")

RETURN ELEMENTS_COUNT(page, "[data-testid='dynamic-product-card']")
