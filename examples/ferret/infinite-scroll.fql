LET baseUrl = @baseUrl
LET page = DOCUMENT(baseUrl + "/scenarios/infinite-scroll/", { driver: "cdp" })

WAIT_ELEMENT(page, "[data-testid='infinite-products'] [data-testid='product-card']")

RETURN ELEMENTS_COUNT(page, "[data-testid='product-card']")
