LET baseUrl = @baseUrl
LET page = DOCUMENT(baseUrl + "/scenarios/network/", { driver: "cdp" })

CLICK(page, "button[data-fetch='products']")
WAIT_ELEMENT(page, "#network-log li")

RETURN INNER_TEXT(ELEMENT(page, "#network-log li:last-child"))
